import { AlternatecasesPipe } from './alternatecases.pipe';

describe('AlternatecasesPipe', () => {
  it('create an instance', () => {
    const pipe = new AlternatecasesPipe();
    expect(pipe).toBeTruthy();
  });
});
